# Environment / secrets
.env
.env.*
!.env.example
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Node
node_modules/

# Data / build artifacts
data/
*.faiss
*.jsonl

# OS
.DS_Store

# macOS AppleDouble metadata (created by tar/scp from a Mac)
._*

# Build logs
logs/

# HuggingFace model cache (deploy/setup.sh places it inside the app dir so
# systemd ProtectHome cannot hide it from the service)
.cache/
